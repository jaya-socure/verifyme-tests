{
  "name": "verifyme-tests",
  "version": "1.0.0",
  "description": "Standalone test suite for the VerifyMe API (runs against the live hosted API)",
  "scripts": {
    "test": "jest --verbose"
  },
  "jest": {
    "testTimeout": 30000
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
